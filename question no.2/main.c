#include <stdio.h>
float do_it ( 'a','b','c')//were a, b, c are characters
void print_a_number(intx)//were x which is an integer argument
int main()
{
    printf("Hello world!\n");
    return 0;
}
