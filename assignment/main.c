#include <stdio.h>
 int x=1;//it is not : but we use ;

int main()
{ if(x==1)// there is no termimantion and its == but not =
   {

    printf(" x equals 1");
   }else{//not otherwise
       printf(" x does not equal 1");

   }
    return 0;
}
